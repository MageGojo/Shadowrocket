# 进度

## 已完成

- 新建项目目录 `shadowrocket-rules`。
- 创建目录结构：
  - `config/`
  - `data/`
  - `docs/`
- 使用 Surge 下载上游规则到 `config/sr_ad_only.conf`。
- 复制上游规则为 `config/custom.conf`，作为后续自定义输出。
- 从本机 Shadowrocket 沙盒复制数据库到 `data/sr_ad_only.db`。
- 确认数据库可读取，并识别出规则表结构。
- 编写项目 README、需求说明和技术设计文档。
- 在 `custom.conf` 新增自定义 `[Proxy Group]`（OpenAI / Gemini / Claude / GoogleAI / GoogleSearch / Apple / Domestic / Final）。
- 在 `[Rule]` 前置 AI / Google / Apple 分流规则，后接上游广告 `Reject` 规则。
- 在 `custom.conf` 文件开头新增 `[General]` 段，配置 TUN 旁路路由（`bypass-system` / `skip-proxy` / `tun-excluded-routes`）。
- 校验 `custom.conf` 整体结构：`[General]` → `[Proxy Group]` → `[Rule]`，无重复段。
- 重构策略组地区筛选：每个平台改为各自“专属地区”（OpenAI/Claude/GoogleAI=美国，Gemini=日本，GoogleSearch=就近香港/台湾/日本/新加坡）。
- 收紧地区正则：用旗帜 emoji / 中文名 / 带词界 `\b` 的英文代码匹配，修复 `US` 误配 `Plus` 及误落“剩余流量”信息节点的问题。
- 每个平台新增自动测速子组 `<平台>-Auto`（`url-test`），并并入主策略组：首页可二选一（Auto 自动 / 手动选节点），默认 Auto（Apple 默认 DIRECT）。
- 新增精简版 `config/shcode.conf`：保留 AI 策略组与精确分流，广告 / 海外 / 国内改用 blackmatrix7 远程规则集（jsdelivr CDN），兜底 `FINAL → Final` 默认走节点（Auto 自动选最快）。
- 验证 blackmatrix7 规则集 URL 全部可用（AdvertisingLite / Global / ChinaMax / Telegram / Lan 均 200）。
- `shcode.conf` 扩展分流：新增 `Streaming` 策略组 + 流媒体解锁（Netflix/Disney/HBO/Spotify）、海外社交/开发（Twitter/Instagram/Facebook/Threads/GitHub/Microsoft/Docker）、TikTok 分流、隐私追踪拦截（Privacy→REJECT）；逐条验证规则集 URL（HBOMax/ChinaASN 不存在，已分别用 HBO / ChinaMax 替代）。
- 整理配套去广告 / 解锁模块清单并写入 README（fmz200 blockAds、zirawell allAdBlock/alipayAdBlock、iab0x00 YouTube、shom TikTok 换区均验证 200；RuCu6 YouTube、Keywos TikTok、zirawell 微信单独模块已 404，改用有效替代）。

## 进行中

- 等待用户在 Shadowrocket 中重新导入 `custom.conf` 并验证 TUN 与分流是否生效。

## 待办

- 用户导入后反馈是否生效；如有误杀或地区筛选问题再微调。
- 必要时添加规则生成或校验脚本。
- 视需求决定是否补充流媒体等其它分流规则。

## 下一步

- 按 README 的“重新导入 Shadowrocket”说明导入 `config/custom.conf`。
- 导入后在 App 内打开 TUN/VPN，确认 AI / Google / Apple 分流走对应策略组、广告被拦截。
- 若节点未被策略组识别，检查订阅节点名称是否包含 `US/JP/SG/TW/KR/HK` 等地区标识。

